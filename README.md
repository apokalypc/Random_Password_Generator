# Random Password Generator (Python)

## What It Does
A simple Python program that generates a strong random password using letters, numbers, and symbols.

## Features
- Randomly generated secure password
- Uses letters, digits, and special characters
- Easy to run from the terminal

## How to Run
1. Make sure Python 3 is installed.
2. Clone the repository
